server:
  port: 9002

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mortynation/config-server
          clone-on-start: true